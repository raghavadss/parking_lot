var assert = require('assert');
var parkingManager = require('../parking-manager');

describe('parking_manager', function() {
  describe('create parking lot', function() {
    it('should not create when input is NaN', function() {
      
    });
  });
});